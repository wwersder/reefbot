package com.reefbot.repository;

import com.reefbot.entity.PlayerSlotWarState;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlayerSlotWarStateRepository extends JpaRepository<PlayerSlotWarState, Long> {
}
