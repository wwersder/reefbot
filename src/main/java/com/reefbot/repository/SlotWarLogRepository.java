package com.reefbot.repository;

import com.reefbot.entity.SlotWarLog;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SlotWarLogRepository extends JpaRepository<SlotWarLog, Long> {
}
